{
  "servers": {
    //? Create by running `MCP: Add Server...` command in VSCode, then add the command to start your server, e.g. `node dist/index.js`, then type a name for your server, e.g. `my-mcp-server`, and finally select the workspace folder.
    "my-mcp-server-7844a43c": {
      "type": "stdio",
      "command": "node",
      "args": ["dist/index.js", "server:dev"],
      "cwd": "${workspaceFolder}"
      //? Optional: Uncomment and configure for debugging
      // "dev": {
      //   "watch": "dist/**/*.js"
      //   "debug": {
      //     "type": "node"
      //   }
      // }
    }
  },
  "inputs": []
}
